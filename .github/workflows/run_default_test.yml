name: default test

on: [push]

jobs:
  test:

    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install dependencies
        run: |
          sudo apt install iverilog tcsh
      - name: Test default with runtest
        run: |
          cd run
          ../scripts/runtest
